const area = (a) => (a*a);
const perimeter = (a) => (4*a);

//we are exporting the area and perimeter
module.exports.area = area;    //first area is the variable of square.js and second one is from the index.js
module.exports.perimeter = perimeter    //first perimeter is the variable of square.js and second one is from the index.js